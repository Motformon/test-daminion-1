import React from 'react';

type Props = {
	ticket: any;
};

const Ticket: React.FC<Props> = props => {
	// const { ticket } = props;
	// console.log(ticket)
	return (
		<div className='Ticket'>
			
		</div>
	);
}

export default Ticket;